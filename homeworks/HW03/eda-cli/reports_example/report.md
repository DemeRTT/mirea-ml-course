# Example dataset report

Исходный файл: `S02-hw-dataset.csv`

Строк: **41**, столбцов: **5**

## Настройки отчёта

- max_hist_columns: **8**
- top_k_categories: **5**
- min_missing_share: **15%**

## Качество данных (эвристики)

- Оценка качества: **0.70**
- Макс. доля пропусков по колонке: **4.88%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**

- Константные колонки: **False**
- Высокая кардинальность категорий (threshold=50): **False**
- Дубликаты в ID: **True**
  - Колонки: user_id
- Много нулей в числовых (threshold=0.5): **False**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

Порог проблемных пропусков: **15%**

Колонок с долей пропусков выше порога нет.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/` (top-k = 5).

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
